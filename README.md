# databricks-medallion-pipeline
End-to-end Medallion Architecture using Databricks Delta Live Tables (DLT). Implements Auto Loader for ingestion, SCD Type 1 for deduplication, and Materialized Views for Gold-layer KPIs across separate schemas.
